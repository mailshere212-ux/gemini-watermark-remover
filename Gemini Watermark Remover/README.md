# Gemini Watermark Remover (Starter)

This is a tiny starter web app: HTML + CSS + JavaScript.

## Files

- `index.html`: The page structure (what you see).
- `styles.css`: The styling (colors, spacing, layout).
- `app.js`: The behavior (what happens when you upload a file).

## Run it

Open `index.html` in your browser.

